Example .env.development or .env.production

```
VITE_API_URL=http://localhost:9090     # Point to DCS4-Backend
```

```
npm install --force      # Setup with React 19 + Tailwind + Shadcn requires --force
``` 

```
npm run dev              # Run in development mode
npm run build            # Build for production
```